Settings(
    application_port: 8000,
    database: DatabaseSettings(
        username: "app",
        password: "secret",
        port: 5432,
        host: "localhost",
        database_name: "newsletter",
    )
)
